// Package main generates the osx output for the app into the assets directory of this app.

//go:generate go generate ./...


package main

import (
	"github.com/gu-io/gu"
	"github.com/gu-io/osx"
	"{{PKG}}/manifests"
)

// AppSettings defines the attributes for the app for using the GopherJS
// driver.
var AppSettings = gu.AppAttr{
	Name:              "{{Name}}",
	Title:             "{{Name}} Gu App",
	Manifests:         manifests.Manifests,
	Driver:            osx.NewOSXDriver(),
}

func main() {
	app := gu.App(AppSettings)

	app.View(gu.ViewAttr{
		Name:  "View.Index",
        Route: "/*",
    })
}
