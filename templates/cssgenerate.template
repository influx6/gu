// +build ignore

package main

import (
	"encoding/json"
	"fmt"
	"os"
	"strings"
	"path/filepath"

	"github.com/gu-io/gu/parsers/cssparser"
)

var pkgName = {{PKGNAME}}
var pkg = {{PKG_CONTENT}}

func main() {
	cdir, err := os.Getwd()
	if err != nil {
		panic(err)
	}

	items, err := cssparser.ParseDir(filepath.Join(cdir, "./"))
	if err != nil {
		panic(err)
	}

	rd, err := json.MarshalIndent(items.Generate(), "", "\t")
	if err != nil {
		panic(err)
	}


	file, err := os.Create(filepath.Join(cdir, "css.go"))
	if err != nil {
		panic(err)
	}

	defer file.Close()

	pkg = strings.Replace(pkg,"{{PKG}}", pkgName, -1)
	pkg = strings.Replace(pkg,"{{STYLES}}", string(rd), -1)

	file.Write([]byte(pkg))
}